{
  "mcpServers": {
    "testactics-agent-controller": {
      "command": "node",
      "args": [
        "./dist/index.js"
      ],
      "env": {
        "MCP_API_ENDPOINT": "http://localhost:3003",
        "MCP_LOG_DIR": "${shared_work_dir}/logs",
        "MCP_WORK_DIR": "${shared_work_dir}",
        "MCP_DELIVERABLES_DIR": "${deliverables_dir}"
      }
    }
  }
}
